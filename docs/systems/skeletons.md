# 해골 병사 3종 — 검사·해머병·방패병

> 구현 스펙. 수치는 전부 `data/entities.json`(`skeleton_sword` / `skeleton_hammer` / `skeleton_shield`)과
> `data/balance.json`(`poise`, `playerKnockback.slash`, `enemyAi.formation`, `skeleton.boneScatter`, `skeleton.boneThrow`)에서 읽는다.
> 시험: `src/systems/Skeleton.test.ts`. 소환: 몬스터 시험방(`?monsterroom`) 소환 탭에 자동으로 뜬다.
> 2026-09-08 개편: 고블린과 겹치던 기술을 전부 걷어냈고(사용자 지적), 자세(poise)·슈퍼아머를 넣었고, 타격 동작을 팔 중심으로 바꿨다.

## 1. 정체

뼈다귀 병사 한 무리. 고블린이 "개인"이라면 해골은 **소대**다 — 셋이 서로 다른 자리를 맡고, 그 자리가 곧 전투 패턴이다.
고블린과 겹치는 동사(찌르기·내려치기·밀쳐내기)는 기본기 하나씩만 남기고, 나머지는 해골만 하는 것으로 채웠다.

| | 검사 (skeleton_sword) | 해머병 (skeleton_hammer) | 방패병 (skeleton_shield) |
|---|---|---|---|
| 역할 | 측면(flank) — 검무 | 후열(rear) — 지진 | 전열(front) — 벽 |
| 체력 / 체급 | 90 / medium | 200 / **heavy** | 150 / medium |
| 이속 / 사거리 | 4.4 / 2.6 | 2.8 / 3.2 | 3.0 / 3.0 |
| 기본기 (끊긴다) | 베기 48 · **회전 베기 52 (360°)** | — | 방패 너머 찌르기 64 |
| 슈퍼아머 기술 | **찌르기 난무** 26·26·40 · **찔러 들어오기** 56 | **내려치기** 96 · **지진** 64 + 여진 48 | **방패 반격** 52 · **방패 찍기** 44 (가드 부수기) |
| 끊기는 큰 기술 | 뼈 투척 30 (보라) | 달려와 내려치기 100 | 방패 밀어붙이기 56 (빨강) |
| 고유 AI | 백스텝 → 반격 | 후열 대기 → 돌격 | 엄호 · 막히면 반격 |
| 경험치 | 25 | 45 | 35 |

### 공통 규약
- **언데드** — 피가 없다. 피격 파편은 뼛가루, 사망은 뼈 흩어짐(§6).
- **관통은 뼈 사이로** — 권총·플레이어 화살은 `pierceDamageMul 0.5`. 화염구·수류탄·해머는 온전히.
- **자세(poise)** — 셋 다 `interruptible: true`. 슈퍼아머가 없는 기술의 예고·질주는 해머·화살·폭발·스킬에 끊긴다(§2).
- 근접 규약대로 `hitOnContact`. 텔레그래프 문법 그대로(파랑 = 패링, 빨강 = 회피, 보라 = 반사). 슈퍼아머는 **네 번째 신호**(호박색 발광 + 낮은 울림)다 — 세 색과 겹치지 않는다.
- `statusOnHit`/`statusOnBlock` 없음.

## 2. 자세(poise) — 끊김과 슈퍼아머 (공용 시스템, `balance.poise`)

다른 게임의 "슈퍼아머 상태"를 우리 규약으로 옮긴 것. 지금까지 우리 적은 전부 사실상 슈퍼아머였다(해머는 굳히기만, 끊기는 것은 없었다). 이제 **상태**가 있다.

- `EnemyDef.interruptible`(없으면 `poise.defaultInterruptible` = false). 참인 적의 예고(windup)·질주(charging)는 **체력이 예고 시작 때보다 낮아진 틱**에 끊긴다: `recover` `poise.interruptTicks`(36) 동안 튕겨 뻗고(`recoiled` 자세), 콤보는 끝나고, `enemy_interrupted` — 소리 + "공격을 끊었다 — 반격 기회!".
- **총알은 어떤 적도 끊지 못한다**(하드 룰 — 패링 게임을 지우지 않는다). Weapons 의 권총 경로가 기준 체력(`poiseHealthRef`)을 되맞춘다. 끊는 것은 해머·화살·폭발·스킬(구울 물어뜯기 캔슬과 같은 결).
- 끊길 예고에 해머 1·2타가 들어가면 **굳히지 않는다**(굳혔다 끊기면 두 번 멈춘다) — 다음 틱 Enemies 가 끊는다.
- `EnemyAttackDef.superArmor: true` 인 기술은 그 동작(windup~impact) 동안 **끊기지 않고, 해머에 굳지도(attackFreeze) 밀리지도(넉백) 않는다**. 피해는 그대로. 해머가 들어가면 `armored_hit`(호박 불꽃 + 둔탁음 + "슈퍼아머 — 끊기지 않는다, 패링하거나 비켜라").
- 신호: 슈퍼아머 예고는 몸이 **호박색**(`SUPER_ARMOR_TINT`)으로 물들고 예고음 밑에 `armor_up`(낮은 울림)이 깔린다. 마지막 4틱·타격 창은 평소처럼 파랑/빨강.
- 기존 적(고블린 등)은 `defaultInterruptible false` 라 옛 행동 그대로. 전체 게임에 끊김을 켜려면 이 스위치 하나다.

**설계 원칙**: 슈퍼아머 기술에는 반드시 다른 답이 있다 — 파랑이면 패링(완벽 = 처형), 빨강이면 거리. 기본기는 끊겨야 해머로 흐름을 잡을 수 있다.

## 3. 검사 — 검무

| 기술 | 슬롯 | 신호 | 끊김 | 요지 |
|---|---|---|---|---|
| 베기 48 | attack | 파랑 | 끊긴다 | 호 100°, 예고 15. 회전 베기와 교대 |
| **회전 베기 52** | attackAlt (alternate) | 파랑 | 끊긴다 | **호 360°** — 옆·뒤로 돌아간 플레이어도 닿는다. 예고 24 동안 몸을 반대로 감았다 타격에 한 바퀴(그림 = 판정 `strikeProgress`). 뒤로 빠지는 플레이의 벌칙 |
| **찌르기 난무** 26·26·40 | comboAttack ×3 | 파랑 | **슈퍼아머** | 예고 14·8·8, 이음 6. ①②는 `continueOnParry`(일반 패링해도 다음이 온다). 답: 세 번 패링하거나 어느 타든 **완벽 패링 한 번**(무너짐 = 처형 창). 시작되면 해머로 끊을 수 없다. 사거리 안 첫 선택, 쿨 420 |
| **찔러 들어오기** 56 | chargeAttack | 파랑 | **슈퍼아머** | 3.2~6.5m 에서 14 m/s 14틱 달려와 찌른다. 백스텝의 반격이라 다시 휘둘러도 끊기지 않는다 — 패링하라 |
| **뼈 투척** 30 | rangedAttack | 보라 | 끊긴다 | 7m 밖에서 왼팔뼈를 뽑아 던진다(`projectileKind bone`). 반사하면 ×1.5 로 제 몸에, 총·해머로 공중에서 깨진다. 던진 뒤 240틱 **왼팔이 없다**(연출). 쿨 300(`rangedCooldown`) |

**백스텝(evade)** — 추격 중 4.0m 안에서 플레이어의 해머 스윙이 *시작*되면(`weapon.swingSeq` 변화 — 이벤트 구독 없이 상태로) 12틱 2.4m 뒤로 뛴다. 착지 틱에 `wantsCharge` → 찔러 들어오기. 쿨 180. 리듬: **휘두르면 물러나고, 물러났으면 찌른다**.

**대열(flank)** — 방패병이 10m 안에 살아 있으면 편각 ±75°·수렴 2.2m 로 크게 옆으로.

## 4. 해머병 — 지진

| 기술 | 슬롯 | 신호 | 끊김 | 요지 |
|---|---|---|---|---|
| **내려치기** 96 | attack | 파랑 | **슈퍼아머** | 예고 32 의 큰 한 방. 끊을 수 없으니 답은 완벽 패링(처형 창)이나 옆. 헛치면 66틱 |
| **지진** 64 → 여진 48 | comboAttack ×2 | 빨강 | **슈퍼아머** | ① 지면 강타 반경 3.4 → 이음 16틱 → ② **여진: 반경 6.5 의 고리, 안쪽 3.2 는 비어 있다**(`aoeInnerRadius`). ①을 뒤로 빠져 피한 자리(4~6m)가 곧 ②. 답: ①을 옆·뒤로 피한 뒤 ② 전에 **다시 안쪽으로 붙거나 6.5m 밖으로**. 막아도 0.7·밀림 그대로. 바닥에 ①은 원, ②는 띠(aoeBand)가 그려진다. 사거리 안 첫 선택, 쿨 480 |
| 달려와 내려치기 100 | chargeAttack | 파랑 | 끊긴다 | 4.5~9.5m 에서 42틱 달려온 끝에 내려친다. **예고나 질주 중 해머·화살이 박히면 고꾸라진다** — 오는 것을 맞받는 것이 답 |

**체급 heavy** — 마무리 타에 1.08m 만. `blockCannotStagger`.
**대열(rear)** — 방패병이 앞에 살아 있으면 6m 에서 서서 노린다(`holding`, 해머를 어깨에 걸친 준비 자세). 6m 는 돌격 구간이라 **기다리다 쿨마다 달려 들어온다**. 방패병이 죽으면 바로 평소 추격.

## 5. 방패병 — 벽

| 기술 | 슬롯 | 신호 | 끊김 | 요지 |
|---|---|---|---|---|
| 방패 너머 찌르기 64 | attack | 파랑 | 끊긴다 | 호 45°, 짧은 검 |
| **방패 반격** 52 | shieldRiposte | 파랑 | **슈퍼아머** | **해머가 방패에 막힌 그 순간**(Weapons → `wantsRiposte`) 예고 8틱의 찌르기. 방패를 두들기는 것이 위험하다. 쿨 150 — 그 안의 연타는 그냥 막히며 방패만 깎인다(3대 반파·6대 완파는 그대로). 창병의 "4회 막으면 밀쳐내기"는 없다 |
| **방패 찍기** 44 | closeAttack | 빨강 | **슈퍼아머** | **플레이어가 방패를 들고 있을 때만**(`requiresBlocking`) 3.0m 안에서. 방패 모서리로 내리찍는다(방패가 치켜들렸다 앞아래로). 막으면 피해 **그대로**(1.0) + 밀림 1.5배 + **가드 부수기 40틱 굳음**(`guardBreak`, `guard_broken`). 막기가 답이 아니다 — 옆으로. 쿨 240 |
| 방패 밀어붙이기 56 | chargeAttack | 빨강 | 끊긴다 | 4.5~9m 에서 36틱 달려와 몸으로 밀친다. 막아도 5m 밀림 그대로, 피해 절반 |

**정면 방패 150°** — 총·화살·해머를 받아낸다. 열리는 창은 창병과 같다(찌르기~후딜·혼절·부서짐). 총은 뒤·옆에서만.
**엄호(coverAllies)** — 7m 안의 동료가 혼절(처형 창)하면 그 동료와 플레이어 사이 1.3m 로 1.35배속으로 끼어든다(`enemy_cover_start`). **방패병이 있는 무리에서는 방패병을 먼저 치우거나 처형 대상을 멀리서 고른다.**

## 6. 사망 — 뼈 흩어짐

`enemy_died` 에 `main` 이 해골이면 `Stage.spawnBoneScatter`(`balance.skeleton.boneScatter`): 리그의 뼈 메시(두개골·턱·갈비·척추·골반·팔다리·무기·방패)를 **지금 자세 그대로** 월드로 떼어 하나씩 던진다. 죽인 방향에 ×2.0, 회전 ±12 rad/s. 착지하면 **튕긴다**(`Particle.bounce` 세로 0.38·가로 0.55·최대 2회) 뒤 눕고 5.2초 뒤 옅어진다. 뼛가루 조금 + `skeleton_death`. 처형 사망은 해머 접촉까지 리그를 붙잡아 두고 접촉 순간 흩어진다(`executeHoldExtraMs`).

## 7. 외형·동작 (Stage)

- `buildSkeletonBody`: 골반, 척추, 갈비 4대, 쇄골, 두개골+턱, 뼈다귀 다리. 팔은 공용 무기 팔·맨팔. 전부 `flashMaterials`.
- **팔이 치고 몸은 서 있다** — 인간형 기본 동작(예고에 몸 젖힘·타격에 24° 숙임·0.5m 전진)을 해골은 ×0.15(`SKELETON_LEAN_MUL`). 사용자 지적("몸을 기울이니 어색하다").
- 공격마다 팔 스타일이 다르다 — thrust(찌르기 난무·방패 반격·찌르기)는 팔을 수평으로 내지르고(창끝 = `weaponTipDist`), smash(베기·내려치기)는 치켜들었다 내리친다. 회전 베기는 팔을 수평으로 뻗은 채 몸통이 감겼다 한 바퀴 돈다.
- 슈퍼아머 예고: `SUPER_ARMOR_TINT` 호박색. 방패 찍기: 방패가 예고에 치켜들리고 타격에 앞아래로. 뼈 투척 뒤 왼팔 숨김(`armlessTicks`). 뼈 투사체: 마디 둘 달린 막대, 옅은 보라, 빙글빙글. 여진: 띠(`aoeBand`, 안쪽 비율은 정의에서).

## 8. 배치

B2 북동 위병소(방패 1·검사 2), B3 남서·남쪽 위병소(방패·해머·검사 ×2). Zone ELITE 에 방패병·해머병.

## 9. 이벤트

| 이벤트 | 페이로드 | 뜻 |
|---|---|---|
| `enemy_windup` (+`superArmor`) | … | 슈퍼아머 예고면 `armor_up` |
| `enemy_interrupted` | enemyId, enemyType, x, z, ticks | 자세 끊김 |
| `armored_hit` | enemyId, enemyType, x, z, heavy | 슈퍼아머 중 해머 피격(굳지도 밀리지도 않음) |
| `shield_riposte_start` | enemyId, enemyType, x, z | 방패 반격 시작 |
| `guard_broken` | enemyId, enemyType, x, z, ticks | 가드 부수기 성립 |
| `enemy_evade` / `enemy_cover_start` | … | 백스텝 / 엄호 |
| `enemy_combo_start` / `_step` | (공용) | 해골이면 난무·지진 문구 |

## 10. 손잡이

| 느낌 | 어디 |
|---|---|
| 끊김이 너무 헐겁다/빡빡하다 | `poise.interruptTicks`, 기술별 `superArmor` |
| 전 적에 끊김을 켜고 싶다 | `poise.defaultInterruptible: true`(고블린 등 전부 끊기게 된다 — 큰 변화) |
| 검사가 너무 자주 빠진다 | `evade.cooldownTicks` ↑ |
| 여진이 안 읽힌다 | `comboNext.windupTicks`(이음 뒤 예고) ↑, `aoeInnerRadius` ↑ |
| 방패병 반격이 너무 잦다 | `shieldRiposte.cooldownTicks` ↑ |
| 뼈 투척이 성가시다 | `rangedAttack.cooldownTicks` ↑, `minRange` ↑ |
